import { TextTrimDirective } from './text-trim.directive';

describe('TextTrimDirective', () => {
  it('should create an instance', () => {
    const directive = new TextTrimDirective();
    expect(directive).toBeTruthy();
  });
});
