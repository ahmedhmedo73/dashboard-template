import { ArStringOnlyDirective } from './ar-string-only.directive';

describe('ArStringOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new ArStringOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
