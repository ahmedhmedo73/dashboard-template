import { IdentityIdDirective } from './identity-id.directive';

describe('identityIdDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentityIdDirective();
    expect(directive).toBeTruthy();
  });
});
